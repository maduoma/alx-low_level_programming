#include "main.h"

/**
 * _islower - Returns 1 if it is lower 
 * or 0 otherwise
 * Return: Always 0
 */
int _islower(int c)
{
	if (c >= 97 && C <= 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
